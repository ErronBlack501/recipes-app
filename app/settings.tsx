import { Text, SafeAreaView } from "react-native";

const Settings = () => {
  return (
    <SafeAreaView>
      <Text>Hello from settings!</Text>
    </SafeAreaView>
  );
};

export default Settings;
